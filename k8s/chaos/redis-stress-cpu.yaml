apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: redis-cpu-stress
  namespace: vk
spec:
  mode: one
  selector:
    namespaces: [vk]
    labelSelectors:
      app.kubernetes.io/name: redis-cluster
  stressors:
    cpu:
      workers: 4
      load: 100
  duration: "30s"
