image:
  tag: 7.2.7-debian-12-r0

usePassword: false

cluster:
  nodes: 6
  replicas: 1

redis:
  configmap: |
    maxmemory 1gb
    maxmemory-policy allkeys-lru

persistence:
  size: 5Gi

metrics:
  enabled: true
  serviceMonitor:
    enabled: true
    labels:
      release: monitoring
